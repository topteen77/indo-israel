# Host port for the app (nginx). Default 80.
# Production at http://43.204.127.118:8081: set APP_PORT=8081 in .env and open TCP 8081 in the server firewall/security group.
# APP_PORT=80
# APP_PORT=8080
# APP_PORT=8081

# Backend (optional, for non-Docker runs)
# PORT=5000

# Frontend - API base URL (used at build time for Next.js)
# - Omit or "http://localhost:5000" for local dev (frontend talks to backend on 5000)
# - "" (empty) when using nginx reverse proxy: /api is same-origin, nginx proxies to backend
# NEXT_PUBLIC_API_URL=
